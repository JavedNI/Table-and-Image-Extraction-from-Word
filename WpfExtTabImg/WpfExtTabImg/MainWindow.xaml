<Window x:Class="WpfExtTabImg.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="200">
    <StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height=" *"/>
                <RowDefinition Height=" *"/>
            </Grid.RowDefinitions>

            <Button Margin="10 25 10 0" x:Name="ExtImgButton" Click="ExtImgButton_Click" Content="Extract Images" Grid.Row="0" />
            <Button Margin="10 25 10 0" x:Name="ExtTabButton" Click="ExtTabButton_Click" Content="Extract Tables" Grid.Row="1" />

        </Grid>
    </StackPanel>
</Window>
